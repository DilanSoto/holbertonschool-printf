.TH _PRINTF  "2023-11-26" "_PrintF MAN PAGE"
.SH NAME
_printf - Custom print function

.SH SYNOPSIS
.B #include <stdio.h>
.br
.B #include <stdarg.h>
.br
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf
function is a simplified version of the
.B _printf
function in the C programming language. It takes a format string and a variable list of arguments, and prints the formatted output to the standard output.

.PP
The format string can contain format specifiers, such as
.B %d
for integers,
.B %f
for floating-point numbers, and
.B %s
for strings.

.SH RETURN VALUE
The
.B _printf
function returns the number of characters printed (excluding the null-terminating character), or a negative value if an error occurs.

.SH EXAMPLES
The following example shows how to use the
.B _printf
function to print an integer and a string:

       _printf("Character: %c\n", 'A');
        This example prints "A"

       _printf("String: %s\n", "Hello, World");
        This example prints "Hello, World"


.SH AUTHOR
This manual was written by [Dilan Soto & Javier Marrero]
